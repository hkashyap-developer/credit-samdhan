export interface blogCard {
    title: string,
    image:any,
    slug: string, 
    excerpt: string, 
    publishedAt: string, 
}

export interface singleBlogPost {
    title: string,
    slug: string, 
    full_image:any,
    publishedAt: string,     
    details: string, 
}